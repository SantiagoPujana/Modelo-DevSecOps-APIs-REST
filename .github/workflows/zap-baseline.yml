name: DAST (OWASP ZAP Baseline)

on:
  workflow_call:
    outputs:
      security_status:
        description: "Security result for this phase: passed|failed"
        value: passed

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "DAST ready for phase execution"